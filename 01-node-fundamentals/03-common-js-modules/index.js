// const add = require('./math.js');
// console.log(add(5, 3));

// =====================================================================
// =====================================================================

// const math = require('./math.js');
// console.log(math.add(5, 3));
// console.log(math.sub(5, 3));

// =====================================================================
// =====================================================================

// const { add, sub } = require('./math.js');
// console.log(add(5, 3));
// console.log(sub(5, 3));

// =====================================================================
// =====================================================================

// const { addFn, subFn } = require('./math.js');
// console.log(addFn(5, 3));
// console.log(subFn(5, 3));

// =====================================================================
// =====================================================================

// const { addEx, subEx } = require('./math.js');
// console.log(addEx(5, 3));
// console.log(subEx(5, 3));

// =====================================================================
// =====================================================================

const math = require('./math.js');
console.log(math.addEx(10, 11));
console.log(math.subEx(10, 11));
console.log(math.mulEx(10, 11));